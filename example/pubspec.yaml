name: better_chat_scrolling_ai_example
description: Example app demonstrating better_chat_scrolling_ai package.

publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  better_chat_scrolling_ai:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
